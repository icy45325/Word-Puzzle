node_modules/
.expo/
dist/
web-build/
*.log
.DS_Store
.env*
!.env.example

# Native projects are regenerated by `expo prebuild` (see scripts/build-apk.sh)
android/
ios/

# Release signing artifacts (must stay local; never commit keystore or passwords)
release.keystore
keystore.properties

